# 设计一个支持十万连接的聊天服务器。

## 前提

要分清是什么样的连接，是十万连接里只有少部分活跃，还是同时活跃，一对一还是群聊，单机十万还是集群十万。

## 不同场景的主要矛盾（先简单理解一下）

| 场景                   | 主要问题                                     |
| ---------------------- | -------------------------------------------- |
| 十万连接，大部分不活跃 | 文件描述符、内存、心跳、连接管理             |
| 十万连接同时活跃       | CPU、网卡带宽、系统调用、锁竞争、发送积压    |
| 一对一聊天             | 如何找到接收方所在服务器，保证消息可靠和有序 |
| 普通群聊               | 消息扇出，群成员越多，发送量越大             |
| 万人大群               | 不能简单逐个连接推送，需要分层广播和限速     |
| 单机十万               | 单机资源限制、事件循环效率、故障风险         |
| 集群十万               | 连接路由、跨节点通信、扩容、故障转移         |

解决这些问题，都是要解决

- 服务器能不能低成本维持这十万连接
- 消息产生后，怎么可靠正确地送到正确的连接。

### 部分活跃，大部分不活跃的十万

- 不能一个连接一个线程，不然光这个上下文切换都是很大的开销。应该用非阻塞Socket+epoll+Reactor事件驱动模型。不过这样只是不占用CPU了，该有的消耗（内存资源占用还是有的）
- 得设置文件描述符限制，进程的 `ulimit -n`，listen的队列长度，SYN半连接队列
- 然后需要控制每条连接的内存，读写缓冲区啥的（其实就是一个连接类，类里有几个写好的变量，不要设置的很大），可以用内存池，共享缓冲区

### 都活跃的十万

- 消息太多了，消息的接收和发送开销很大，主要受到网卡带宽的影响
- 每次来消息都得解析消息协议，系统调用，线程同步等等，都要用到CPU。就要减少CPU的使用，可以批量读取和发送，减少锁，减少内存复制（零copy）
- 还要考虑有客户端网速很慢（慢客户端），一直收不到消息，消息一直累积在服务器，会拖垮服务器。那么这时候可以搞消息队列，能存的消息满了，后面的就搞限流，熔断。（这就是背压机制）

## 大群聊

- 有消息扇出的问题，下行流量很大
- 主要还是用分层广播的方式解决



## 其他问题

对于大量的连接来说，不适合用单机（就是只有一个服务器进行服务），还是得用集群，不然一个服务器挂了，服务直接不可用了，不合理。

用集群也尽量每次请求打到同一个服务器上，降低状态记录资源的消耗。

还要处理失效的连接（心跳检测）


## 面试回答

```
我会先确认十万连接的具体含义，因为十万条大部分空闲的连接，和十万条同时活跃的连接，压力完全不同；一对一和万人群聊也不同；还要区分单机十万和集群十万。

如果十万连接大部分只是在线，主要问题是文件描述符、连接内存、Socket 缓冲区和心跳管理。我会使用非阻塞 Socket、epoll 和 Reactor 模型，避免一个连接一个线程。通常每个 CPU 核心设置一个事件循环，每条连接固定由一个 IO 线程管理，同时调整文件描述符上限和网络参数，并严格控制单连接缓冲区大小。

如果十万连接同时活跃，主要问题会转变为 CPU、网卡带宽、消息复制和发送积压。这时要减少系统调用和内存复制，将复杂业务移出 IO 线程，并为每条连接设置有上限的发送队列，通过背压处理慢客户端。

一对一聊天需要维护 user_id 到 Gateway 和连接的路由关系。发送方和接收方在不同节点时，由消息服务查询路由并转发到对应 Gateway。消息需要业务层 message_id、确认和幂等机制，因为 TCP 可靠只能代表字节送到对方 TCP 栈，不能代表业务已经处理。

群聊的核心是消息扇出。小群可以按在线成员所在 Gateway 分组推送；万人群不能由中心节点逐个用户发送，而应该把一条消息广播到相关 Gateway，再由 Gateway 向本机连接推送，并配合限流、合并和降级。

生产环境中我不会把十万连接集中在一台机器上，而是部署 Gateway 集群，由四层负载均衡分配连接。在线路由通过带 TTL 的注册信息维护，节点宕机后客户端重连。后端使用消息队列进行削峰和解耦，同一会话按照会话 ID 分区，从而保证会话内有序。

最后还需要完整的连接数、事件循环延迟、发送队列、消息积压和端到端延迟监控，并在过载时执行限流、拒绝新连接和关闭慢连接。
```









# 一个服务CPU突然升到100%，你如何排查？

这样的问题其实就是说，“我知道程序出问题了，占用了大量CPU，可是具体怎么排查是哪个进程，哪个线程，哪段代码，什么条件导致的呢？”

## 找哪个进程

```shell
top
然后按大写的P
这样就看到正在运行的进程，然后按照CPU 使用率排序
```

![image-20260801143419525](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20260801143419525.png)

## 判断是用户代码还是内核导致的

```shell
pidstat -p $(pidof cpu_bug) 1   #这个cpu_bug是占用CPU最高的那个程序的名字

可能看到：

UID       PID    %usr   %system   %CPU   Command
1000    12345   99.5      0.2    99.7   cpu_bug
```



## 找到哪个线程

执行：

```
top -H -p $(pidof cpu_bug)
```

其中：

```
-H    把线程分别显示出来
-p    只观察指定进程
```

![image-20260801143624152](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20260801143624152.png)

里面有仨线程，其中名字为hot-worker的线程占用了大部分CPU



## 找到线程正在执行的函数（用GDB）

```shell
sudo gdb -q -p $(pidof cpu_bug)
```

GDB 附加成功后，进程会暂时停下来。

```shell
查看所有线程
info threads
```

![image-20260801144245901](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20260801144245901.png)

看到对应的hot-worker的线程是3

```
thread 3  # 转到第三个线程
bt		  # 查看调用栈
```

查看当前代码：

```
frame 0
list
```

![image-20260801144602526](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20260801144602526.png)

这样就定位到代码了



## 退出调试过程

```
detach
quit
```



## 解决办法

上面的过程能定位到是哪里出了问题，然后需要解决了

思想是先止损，再修复

- 回滚版本
- 熔断故障下游
- 关闭异常功能

先记住这些吧



# 服务延迟突然变高，但CPU和内存正常，怎么定位？

CPU和内存正常，只能说明没有占用计算资源或者内存资源，但是不能说明没有等待的问题

线程可能都在等待：

- 网络；
- 磁盘；
- 锁；
- 下游服务；
- 数据库连接；
- 线程池任务；
- DNS；
- 连接池资源。

用strace命令持续追踪状态，多次触发导致延迟的操作，在程序执行卡住的时候GDB一下，然后看调用栈，看谁拿着锁呢或者怎么着，定位后再做修改。实际过程中因为这个操作不大可能人工快速完成，所以一般是写个脚本，一直抓状态，一直执行操作，GDB也是脚本执行，抓取到结果以后，人为判断哪里有问题。



# 客户端反馈偶发超时，服务端日志没有异常，怎么处理？

“服务端日志没有异常”不能证明服务端没有问题，只能说明现有日志没有记录到问题。

那么问题就变成了找到哪里出了问题。

“客户端超时”可能是：

```
DNS解析超时
TCP连接超时
TLS握手超时
等待连接池超时
请求发送超时
服务端处理超时
读取响应超时
```

也就是说每一段都可能出问题的

需要确认连接有没有到服务端，收到有没有处理连接，处理完有没有正确返回

然后看客户端（这里怎么看，没懂）



## 面试回答

> 客户端偶发超时但服务端日志没有异常时，我首先不会认定服务端正常，而是先明确超时发生在 DNS、建连、TLS、发送请求还是等待响应阶段。
>
> 然后通过 request_id 或 trace_id 对齐客户端、负载均衡、服务端和下游日志。先判断请求是否到达服务端：如果服务端没有收到，要检查网络、负载均衡、listen 队列和 TCP 建连；如果收到但没有完成，要检查线程池排队、锁、连接池和下游调用；如果服务端已经返回，则检查响应写入、代理超时、客户端超时设置和 TCP 重传。
>
> 我还会核对客户端、负载均衡、服务端和下游各层的超时时间是否冲突，并查看问题时间点的 P99、线程池队列、连接池等待、文件描述符、TCP 重传和 listen drop，而不是只看平均值。
>
> 如果应用日志仍无法判断，就在客户端、负载均衡或服务端抓包，确认请求和响应实际走到了哪一步。



